version: '3.7'

services:
    # backend
    backend:
        container_name: tapgives_api
        build: ./backend
        command: uvicorn app.main:app --reload --host 0.0.0.0 --port 80 # && chmod +x ./prestart.sh
        volumes:
            - ./backend/:/app/
        ports:
            - "${WEB_PORT}:80"
        env_file:
            - .env
        depends_on:
            - ${POSTGRES_SERVER}
            - cache
    
    # database
    tapgives_db:
        container_name: tapgives_db
        image: postgres:alpine
        volumes:
            - data:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=${POSTGRES_USER}
            - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
            - POSTGRES_DB=${POSTGRES_DB}
            - PGPORT=${POSTGRES_PORT}
        ports:
            - "${POSTGRES_PORT}:5432"
        
    # cache
    cache:
        container_name: tapgives_rds
        image: redis:alpine
        ports:
            - "${REDIS_PORT}:6379"
        restart: "always"

volumes:
  data: